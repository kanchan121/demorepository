#include<stdio.h>
int main()
{
int num,rem,sum=0,t;
printf("enter number");
scanf("%d",&num);
t=num;
while(num>0)
{
rem=num%10;
sum=sum*10+rem;
num=num/10;
}
//num=temp;
if(sum==t)
{
printf("%d is palindrome",sum);
}
else
printf("not palindrome");
}
